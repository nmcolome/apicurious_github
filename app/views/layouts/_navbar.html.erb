<% if current_user %>
  <div class="navbar">
    <%= "Welcome #{current_user.nickname}" %>
    <%= link_to "Sign out", logout_path, class: "nav_links" %>
  </div>
<% else %>
  <div class="navbar">
  <%= link_to "Sign in with Github", github_login_path %>
  </div>
<% end %>